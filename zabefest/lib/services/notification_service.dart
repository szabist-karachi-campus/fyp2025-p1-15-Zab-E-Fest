// Temporary simplified notification service for APK build
// Remove this comment and restore original implementation after successful build

class NotificationService {
  Future<void> initialize() async {
    // Empty implementation for APK build
  }

  Future<void> showLocalNotification({
    required String title,
    required String body,
  }) async {
    // Empty implementation for APK build
    // This would show notifications in the full implementation
  }
}